#include <stdio.h>
#include <math.h>

int X0, X1, Y0, Y1, Z0, Z1;
float dist;

int main(void) {
  while(){
    printf("Digite o x da coordenada:\n");
    scanf("%d", &X0);
    printf("Digite o Y da coordenada:\n");
    scanf("%d", &Y0);
    printf("Digite o x da coordenada:\n");
    scanf("%d", &X1);
    printf("Digite o Y da coordenada:\n");
    scanf("%d", &Y1);
    if( X0 == -99){
      break;
    }
    else{
      dist = dist + sqrtf(pow( ,2)+pow( ,2))
    }
  }
  return 0;
}